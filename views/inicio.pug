extends ./layout/app

block styles
    link(rel="stylesheet" href="https://unpkg.com/leaflet@1.8.0/dist/leaflet.css")

block contenido 
    div(class="py-5")
        h1(class="text-center text-4xl font-bold") Ubicación en el Mapa

        div(class="flex flex-col md:flex-row items-center py-10 gap-4") 
            h2(class="text-sm uppercase text-gray-800 font-bold") Filtar Propiedades

            div(class="w-full md:w-auto flex items-center gap-2")
                label(for="categorias" class="text-sm w-24 uppercase text-gray-500 font-bold") Categorias
                select(id="categorias" class="flex-1 bg-white w-full px-3 py-2 border border-gray-300 rounded-md shadow")
                    option(value="") - Seleccione -
                    each categoria in categorias
                        option(value=categoria.id)= categoria.nombre

        //div(class="w-full md:w-auto flex items-center gap-2")
                    label(for="precios" class="text-sm w-24 uppercase text-gray-500 font-bold") Precios
                    select(id="precios" class="flex-1 bg-white w-full px-3 py-2 border border-gray-300 rounded-md shadow")
                        option(value="") - Seleccione -
                        each precio in precios
                            option(value=precio.id)= precio.nombre 

        div(id="mapa-inicio" class="h-[600px] mt-5" )

        section(class="py-5")
            h2(class="text-center text-4xl font-semibold pt-10") Casas en Venta

            div(class="grid grid-cols-1 lg:grid-cols-3 gap-5 mt-14")
                each propiedad in casas
                    include ./templates/card
        

        section(class="py-5")
            h2(class="text-center text-4xl font-semibold pt-10") Departamentos en Venta

            div(class="grid grid-cols-1 lg:grid-cols-3 gap-5 mt-14")
                each propiedad in departamentos
                    include ./templates/card



    block scripts
        script(src="https://unpkg.com/leaflet@1.8.0/dist/leaflet.js")
        script(src="https://unpkg.com/esri-leaflet@3.0.8/dist/esri-leaflet.js")
        script(src="/js/mapaInicio.js")
       
block pie 
    footer( class="bg-white rounded-lg shadow m-4 dark:bg-gray-800 w-full mx-auto max-w-screen-xl p-4 md:flex md:items-center md:justify-betweenspan.text-sm.text-gray-500.sm:text-center.dark:text-gray-400") #{new Date().getFullYear()} 
      a(href="/" class="hover:underline") Ubiko | Todos los derechos reservados
      ul( class="flex flex-wrap items-center mt-3 text-sm font-medium text-gray-500 dark:text-gray-400 sm:mt-0")
        li
            a(href="#" class="hover:underline me-4 md:me-6") About
        li
            a(href="#" class="hover:underline me-4 md:me-6") Privacy Policy
        li
            a(href="#" class="hover:underline me-4 md:me-6") Licensing
        li
            a(href="#" class="hover:underline") Contact
