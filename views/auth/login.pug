extends ../layout/index

block contenido
    div(class="flex justify-between items-center")
                a(href="/" class="text-4xl my-5 font-extrabold text-blue-700") Ubiko
                a(href="/" class="text-sm.my-5 font-extrabold text-gray-100") volver al inicio
    .py-0
        h2.text-center.text-2xl.font-extrabold.text-indigo-600= pagina

        if errores
            div(class="max-w-md mx-auto my-10")
                each error in errores
                    p.bg-red-600.text-white.uppercase.text-xs.text-center.p-2.mb-1.font-bold= error.msg

        .mt-8.mx-auto.max-w-md
            .bg-gradient-to-r.from-blue-400.to-white.shadow-lg.rounded-lg.p-8
                form(action="/auth/login" method="POST")
                    input(type="hidden" name="_csrf" value=csrfToken)
                    
                    // Input for Email
                    div(class="mb-6")
                        label.block.text-sm.uppercase.text-gray-500.font-semibold(for="email") Gmail
                        input#email.w-full.px-4.py-3.border.border-gray-300.rounded-lg.text-gray-900.placeholder-gray-400(placeholder="you@example.com", type="email", name="email", required)

                    // Input for Password
                    div(class="mb-6")
                        label.block.text-sm.uppercase.text-gray-500.font-semibold(for="password") Password
                        .relative
                            input#password.w-full.px-4.py-3.border.border-gray-300.rounded-lg.text-gray-900.placeholder-gray-400(placeholder="*******", type="password", name="password", required)
                    
                    // Google reCAPTCHA
                    .mb-6
                        .g-recaptcha(data-sitekey="6LcbHHIqAAAAAO_keLm4HZlF9QfrIZ_arJVbMQLb")

                    // Submit Button
                    input(class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 cursor-pointer" type="submit" value="Iniciar Sesión")
                    
                    // Links for Forgot Password and Register
                    div(class="flex items-center justify-between mt-4")
                        a(href="/auth/registro" class="text-indigo-600 hover:text-indigo-800 text-xs") ¿Aún no tienes una cuenta? Crear Cuenta
                        a(href="/auth/restaurar-password" class="text-indigo-600 hover:text-indigo-800 text-xs") Olvidé mi contraseña

block scripts

block pie
    div
        h2(class="text-center text-sm text-gray-400 py-2") &copy; TODOS LOS DERECHOS RESERVADOS #{new Date().getFullYear()} | by Ubiko  &hearts;
